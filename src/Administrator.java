public class Administrator extends User {

    public static void addBal(double amount) { //Top up credits for customer
        TextEditor TopUp = new TextEditor();
        TopUp.fileReader(TextEditor.FilePaths.USERS, );
    }
}